build/
*.apk
*.idsig
# телеметрия — секрет держим приватным, никогда не коммитим
assets/telemetry.b64
telemetry_secret*
# Входы генератора assets/models.tsv: дампы параметров и выгрузка телеметрии
# (в ids.json — серийные номера и IP пользователей). Не публикуем; в APK они и так
# не попадают (aapt2 пакует только assets/). Собранный models.tsv — коммитится.
data/
